[gd_scene format=3 uid="uid://dnwdwa7a4altr"]

[ext_resource type="Texture2D" uid="uid://ban32l63owb3k" path="res://icon.svg" id="1_4flbx"]

[sub_resource type="GDScript" id="GDScript_onrkg"]
script/source = "extends CharacterBody2D

const SPEED = 400.0
const JUMP_VELOCITY = -800.0
const GRAVITY = 2000.0
const ROTATION_SPEED = 3.75

@onready var sprite = $Sprite2D

func _physics_process(delta: float) -> void:
	# Horizontální pohyb
	velocity.x = SPEED

	# Gravity + dopad
	if not is_on_floor():
		velocity.y += GRAVITY * delta
	else:
		velocity.y = 0
		
		# zarovnání na 90°
		var angle = sprite.rotation
		var snapped = round(angle / (PI / 2)) * (PI / 2)
		sprite.rotation = snapped

	# Jump
	if Input.is_action_just_pressed(\"ui_accept\") and is_on_floor():
		velocity.y = JUMP_VELOCITY

	# Rotace ve vzduchu
	if not is_on_floor():
		sprite.rotation += ROTATION_SPEED * delta

	move_and_slide()

func Explode():
	queue_free()
"

[sub_resource type="RectangleShape2D" id="RectangleShape2D_i3pqv"]
size = Vector2(128, 128)

[node name="Player" type="CharacterBody2D" unique_id=1337890187]
script = SubResource("GDScript_onrkg")

[node name="Sprite2D" type="Sprite2D" parent="." unique_id=680774025]
texture = ExtResource("1_4flbx")

[node name="CollisionShape2D" type="CollisionShape2D" parent="." unique_id=1377382138]
shape = SubResource("RectangleShape2D_i3pqv")
